# VASTSDK — Android

Not started.

Planned to mirror the Apple split: a pure-logic core (parsing, wrapper chains,
pod scheduling, the tracking engine) that takes no dependency on a player, and a
kit layer that binds it to ExoPlayer and the ad UI. The correctness rules and the
test fixtures come from [../Apple](../Apple), which already encodes them.
