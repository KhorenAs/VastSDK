# VASTSDK — Web

Not started.

Planned as TypeScript over HTML5 `<video>`, with the same split the Apple SDK
uses: parsing, wrapper chains, pod scheduling and tracking as player-independent
logic, and a thin layer binding it to the element and the ad UI. The correctness
rules and the test fixtures come from [../Apple](../Apple), which already encodes
them.
